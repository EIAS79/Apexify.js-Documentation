# Scene — overview {#00-scene-composition}


This page is the **single entry point** for Scene: a **declarative layer graph** — **width**, **height**, optional **background**, and an ordered **`layers`** array. **`SceneCreator`** composites everything into **one PNG `Buffer`** in a single pass. From there you can call **`renderScene`**, **`renderSceneToGIF`**, or **`renderSceneToVideoFrames`** depending on whether you need a still, an animated GIF, or a video file.

**Legacy slug:** [Scene hub](/docs#00-scene-hub) still resolves (short redirect file); all content is maintained here.

**Implementation:** **`SceneCreator`** draws the full layer stack to one PNG. **`SceneBuilder`** is a fluent wrapper over the same fields as **`SceneRenderInput`**. Optional **`validateSceneRenderInput`** runs before rasterizing unless you pass **`{ validate: false }`**. Scene→GIF and scene→video helpers call the same composition step, then encode frames.

**Higher-level building blocks** (templates **`{{…}}`**, **`painter.assets`** **`$…`**, preset **`painter.components`**, plugins) are documented in the **[Composition hub](/docs#00-composition-hub)** — Scene consumes their output as normal **`SceneRenderInput`** / **`SceneLayer[]`**.

**Types:** **`SceneRenderInput`**, **`SceneLayer`**, **`SceneRenderOptions`** — import from the package (**`import type { … } from "apexify.js"`** or **`apexify.js/types`**).

**On `ApexPainter`:** `createScene`, `renderScene`, `renderSceneToGIF`, `renderSceneToVideoFrames`, **`validateSceneRenderInput`**.

### Builder & render extras

- **`addLayers([...])`** — append many layers at once; **`addLayer(x)`** is equivalent to **`addLayers([x])`**.
- **Layer stack editing:** **`insertLayer`**, **`insertLayers`**, **`removeLayer`**, **`moveLayer`**, **`replaceLayers`**, **`clearLayers`**, read **`layerCount`**.
- **`toRenderInput()`** — returns a plain **`SceneRenderInput`** (copied **`layers`** array) for **`renderScene`**, GIF/video helpers, caching, or tests.
- **Validation:** default **`renderScene`** / **`SceneBuilder.render`** runs **`validateSceneRenderInput`** (dimensions + max nested **`surface`** depth). Pass **`{ validate: false }`** only for trusted hot paths; tune depth with **`{ maxSurfaceDepth: n }`**.
- **GIF / video:** optional **`sceneRender`** on the second argument forwards the same options to the inner scene PNG step.

---

## What you get in one sentence each

| Output | API | Idea |
|--------|-----|------|
| **PNG** | **`renderScene`** or **`SceneBuilder.render()`** | Final poster / card / dashboard bitmap. |
| **GIF** | **`renderSceneToGIF`** | Renders the scene once, optionally **prepends** that frame, merges **`gifFrames`** (with **`repeat`**), runs **`createGIF`**. |
| **Video** | **`renderSceneToVideoFrames`** | Same composed PNG, merged with **`createFromFrames`** + optional **`framesWithRepeats`** → FFmpeg. |

---

## When to use Scene

| Goal / situation | Prefer |
|-----------------|--------|
| One background + a couple of images/text | **`createCanvas`** + **`createImage`** / **`createText`** — fewer moving parts. |
| Many layers, **charts as layers**, **nested cards** (`surface`), one export | **Scene** |
| Same visual design → **GIF** or **MP4** | **`renderSceneToGIF`** / **`renderSceneToVideoFrames`** after designing **`layers`**. |

Scene reuses the same engines as the rest of the library (**`@napi-rs/canvas`**, **`ChartCreator`**, **`ImageCreator`**, **`TextCreator`**, **`Path2DCreator`**, **`CanvasCreator`** for backgrounds).

---

## Chapter guide (`01`–`09`)

Read in order the first time; later jump by topic.

| # | Topic | Link |
|---|--------|------|
| 1 | **`SceneRenderInput`**, **`SceneRenderOptions`**, **`SceneBuilder`** (`addLayers`, stack edits, **`toRenderInput`**), **`createScene`** | [Types & builder](/docs#01-scene-types-builder) |
| 2 | **`image`**, **`text`**, **`path`**, **`imageBuffer`** — variants & examples | [Raster & vector layers](/docs#02-scene-layers-raster-text-path) |
| 3 | **`chart`**, **`chartComparison`**, **`chartCombo`** | [Chart layers](/docs#03-scene-layers-charts) |
| 4 | **`customLines`**, **`surface`** (nesting, placement) | [Connectors & surfaces](/docs#04-scene-layers-custom-lines-surface) |
| 5 | **`background`** as **`CanvasConfig`** (color, gradient, layers, transparency) | [Scene backgrounds](/docs#05-scene-backgrounds) |
| 6 | **`renderScene`** — object vs builder, reuse, **`$`** resolution | [Render scene](/docs#06-render-scene) |
| 7 | **`renderSceneToGIF`** — prepend, repeat, `onStart` rule | [Scene → GIF](/docs#07-render-scene-to-gif) |
| 8 | **`renderSceneToVideoFrames`** — `createFromFrames`, `framesWithRepeats` | [Scene → video](/docs#08-render-scene-to-video) |
| 9 | Paint order, performance, pitfalls | [Paint order & performance](/docs#09-scene-paint-order-performance) |

---

## Also read · templates, **`$`** assets, components

**[Composition hub](/docs#00-composition-hub)** — **`createTemplate`**, **`painter.assets`**, **`prepareForRender`**, **`painter.components`**, **`$`** defaults on **`renderScene`** vs **`SceneBuilder.render`**. Older bookmark: [templates / assets](/docs#10-templates-assets-components) (section on [Advanced overview](/docs#00-advanced-overview)).

---

## Minimal example

```typescript
import { ApexPainter } from "apexify.js";
import fs from "fs";

const painter = new ApexPainter();

const png = await painter
  .createScene(640, 360)
  .setBackground({ colorBg: "#0f172a" })
  .addLayers([
    {
      type: "text",
      texts: [{ text: "Hello, Scene", x: 48, y: 160, fontSize: 36, color: "#e2e8f0" }],
    },
  ])
  .render();

fs.writeFileSync("./scene.png", png);
```

---

## See also (other doc folders)

- [Canvas overview](/docs#00-create-canvas-overview) — **`CanvasConfig`** / backgrounds used by scenes  
- [Charts overview](/docs#00-charts-overview) — data shapes per **`chartType`**  
- [GIF overview](/docs#00-create-gif-overview) — **`GIFOptions`**, **`GIFInputFrame`**  
- [Video overview](/docs#00-video-overview) — FFmpeg, **`createFromFrames`**  
- [Compose & build](/docs#05-compose-and-build) — **`createVideo`** and assets  

---
