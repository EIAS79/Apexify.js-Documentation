import fs from 'node:fs';import path from 'node:path';const OUT=path.join(process.cwd(),'.doc3-runtime-evidence');const build=JSON.parse(fs.readFileSync(path.join(OUT,'build-bundle.json'),'utf8'));const browser=JSON.parse(fs.readFileSync(path.join(OUT,'browser.json'),'utf8'));const failures=[];if(build.delta.routedManifestJsPercent>15)failures.push(`routed JS regression ${build.delta.routedManifestJsPercent}% exceeds 15%`);if(build.delta.buildPercent>25)failures.push(`build wall regression ${build.delta.buildPercent}% exceeds 25% control tolerance`);if(browser.failures!==0)failures.push(`browser failures=${browser.failures}`);for(const state of browser.states??[]){if((state.axeViolations??[]).length)failures.push(`${state.name} has axe violations`);if(state.horizontalOverflow)failures.push(`${state.name} has horizontal overflow`);}const summary={schemaVersion:1,phase:'DOC-3',status:failures.length?'FAIL':'PASS',thresholds:{routedManifestJsPercentMax:15,buildWallPercentMax:25,axeViolations:0,horizontalOverflow:false},build:build.after,delta:build.delta,browserStates:browser.states?.length??0,failures};fs.writeFileSync(path.join(OUT,'final.json'),`${JSON.stringify(summary,null,2)}\n`);console.log('[doc3-finalize] '+JSON.stringify(summary));if(failures.length)process.exit(1);
