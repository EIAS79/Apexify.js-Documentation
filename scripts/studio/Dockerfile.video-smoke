FROM node:24-bookworm

RUN apt-get update \
  && apt-get install -y --no-install-recommends ca-certificates unzip xz-utils \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY . .

RUN npm ci

CMD ["npm", "run", "studio:smoke:video"]
